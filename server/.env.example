# Use SendGrid for production
SENDGRID_API_KEY=
FROM_EMAIL=hello@yourdomain.com

# Fallback SMTP (e.g., for local testing)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Secret used to sign verification tokens
EMAIL_SECRET=replace-with-a-secure-random-string

# Server port
PORT=3000